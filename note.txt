backend run:
cd .\backend\
node index.js

frontend run:
cd .\frontend\
npm install vite --save-dev //install vite locally
npm run dev

Modifier index.js pr dicter 

besoin deux terminals
ctrl+c = arreter les processus.

Vite/frontend -> port 5173 -> localhost:5173
React/backend -> port 3001 -> localhost:3001

git push -u origin nomdeBranch //-u = future pushes are simpler, i.e you just need to git push

Etiquettes:
[Init] Initialization - Quand on initiliase une base (module ou repo). A utiliser rarement.
[Add] File Addition - Quand un ou plusieurs fichiers sont crees.
[Upd] Update - Quand un fichier est modifie, sans ajout ni suppression.
[Del] Delete - Quand des fichiers doivent etre supprimes du repo.

Process: 
Browser(url) -> component fetch -> backend-> 
index.js routes -> userRoutes -> userController -> 
userModel -> db -> Controller callback -> frontend